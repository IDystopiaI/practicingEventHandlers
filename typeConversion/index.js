// let age = window.prompt("Enter your age: ");

// user input is a string, results in concatenation
// age of 20 -> 201 after concatenation
// age +=1;

/* example showing user input is taken as a string
// can not use augmented assignment here
age = Number(age) + 1;

// age += 1;

console.log(age, typeof age);
*/

let x;
let y;
let z;

x = Number(x);
y = String(y);
z = Boolean(z);

console.log(x, typeof x);
console.log(y, typeof y);
console.log(z, typeof z);
